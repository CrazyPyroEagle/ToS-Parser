namespace ToSParser
{
    public enum PendingInvitationStatus : byte
    {
        PENDING,
        DENIED,
        ACCEPTED,
        FAILED,
        LOADING,
        CANCELED,
        LEFT,
        LOCALE,
        NO_COVEN
    }
}
